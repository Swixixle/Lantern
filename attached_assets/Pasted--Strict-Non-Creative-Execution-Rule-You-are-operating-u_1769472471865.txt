**Strict Non-Creative Execution Rule**

You are operating under a **non-creative, non-interpretive mandate**.

**Do not take creative liberties.**
**Do not make assumptions.**
**Do not infer intent, meaning, hierarchy, or policy.**
**Do not rewire logic or UX for convenience, elegance, or user friendliness.**
**Do not “improve” phrasing, structure, or flow unless explicitly instructed.**

Implement **only** what is specified.

If a requirement is underspecified or ambiguous:

* Stop.
* Ask for clarification.
* Do not fill gaps yourself.

The system’s purpose is **epistemic restraint and claim governance**, not helpfulness or synthesis.

Any deviation from this rule is a defect.

---

I cannot directly view your running Evidence Packet page from here. I can give you a deterministic check that will answer the question unambiguously.

## Deterministic Check

### A) API truth (must be true before UI can be true)

1. Fetch a packet:

```bash
BASE_URL="http://localhost:3000"  # or your deployed URL
PACKET_ID="PASTE_PACKET_ID"
curl -s "$BASE_URL/api/packets/$PACKET_ID" | jq '{packet_id, snapshot_id, snapshot_hash_hex, hash_hex}'
```

Pass condition:

* `snapshot_id` exists and is non-empty
* `snapshot_hash_hex` exists and is non-empty

2. Verify chain endpoint exists and responds:

```bash
curl -s "$BASE_URL/api/packets/$PACKET_ID/verify_chain" | jq
```

Pass condition:

* Response includes keys:

  * `verified_packet_hash`
  * `verified_snapshot_hash`
  * `snapshot_id`
  * `snapshot_hash_match`
  * `claim_in_snapshot_scope`
  * `sources_in_snapshot_scope`

If both API checks pass, the backend contract is satisfied.

---

### B) UI truth (what you should see on `/packets/:packetId`)

On the Evidence Packet page, you must see:

**Snapshot ID:** `<uuid>`
**Snapshot Hash (SHA-256):** `<64-hex>`

And a button labeled exactly:

**Verify Chain**

Clicking it must display the six booleans from `/verify_chain`.

---

## If you want me to confirm “yes” with certainty

Paste:

1. Output from `GET /api/packets/:packet_id` filtered as above, and
2. Output from `GET /api/packets/:packet_id/verify_chain`, and
3. A screenshot of the packet page header area (showing snapshot fields + button)

With those, I can confirm the page shows the snapshot fields and Verify Chain button, and that it’s wired correctly.
